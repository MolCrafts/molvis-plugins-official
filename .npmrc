# Prefer public registry; molrs / molvis-core / molplot are published packages.

# Node prints a Type Stripping ExperimentalWarning once per process that loads
# a .ts config (rsbuild/rslib do). Noise on every build and dev start.
node-options=--disable-warning=ExperimentalWarning
