node_modules/
package-lock.json
npm-shrinkwrap.json
yarn.lock
pnpm-lock.yaml
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example
# dist/ is committed so jsDelivr can serve the plugin from GitHub tags.
# Rebuild with `npm run build` before tagging a release.
